<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name, "アカウント名" %>
      <%= f.text_field :name %>

      <%= f.label :email, "Eメール" %>
      <%= f.text_field :email %>

      <%= f.label :password, "パスワード" %>
      <%= f.password_field :password %>

      <%= f.label :password_confirmation, "再入力" %>
      <%= f.password_field :password_confirmation %>

      <%= f.submit "アカウント作成", class: "btn btn-large btn-primary" %>
      
      <% if @path.is('users#edit') %>
        <%= link_to 'ユーザーを削除する', user_path(current_user), { method: :delete, data: {confirm: '削除してよろしいですか？'} } %>
      <% end %>
    <% end %>
  </div>
</div>
