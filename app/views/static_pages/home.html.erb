<% if logged_in? %>
    <h1>
      <%= gravatar_for current_user %>
      <%= current_user.name %>
    </h1>

    <%#= render 'messages/index' %>

<% else %>
  <div class="center jumbotron">
    <h1>Welcome to the ID Create</h1>

    <%= link_to "Sign up now!", signup_path, class: "btn btn-lg btn-primary" %>
  </div>
<% end %>
